---
'@monitor-sefaz/core': minor
---

O `axios-cookiejar-support` sobe para a v7, que só roda em Node 22 ou superior. Por isso o `@monitor-sefaz/core` passa a exigir Node 22: `engines.node` vai de `>=20` para `>=22`.
