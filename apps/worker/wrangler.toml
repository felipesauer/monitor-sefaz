name = "monitor-sefaz"
main = "src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]

# Deploy: `pnpm --filter @monitor-sefaz/worker deploy` (requer `wrangler login`).
# A SPA aponta para a URL pública deste worker via VITE_API_BASE_URL.
